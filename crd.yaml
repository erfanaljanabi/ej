apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: erfans.demo.local
spec:
  group: demo.local
  names:
    kind: Erfan
    plural: erfans
    singular: erfan
    shortNames:
      - ef
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                message:
                  type: string
