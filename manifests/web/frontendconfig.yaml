apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: erfan-resume-frontend
  namespace: web
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT
